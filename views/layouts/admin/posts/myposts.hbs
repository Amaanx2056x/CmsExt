<html>
{{>links}}
<link rel="stylesheet" href="/css/faicons.css">


<body>
  <div id="wrap">
    {{>header}}
    {{>msgs}}
    <h1 id="headings">MY POSTS</h1>
    {{#if posts}}
    <div style="overflow-x: auto; background: white; opacity: 0.95; border-radius: 5px;">
      <table class='table table-bordered'>
        <thead>
          <tr>
            <th></th>
            <th>File</th>
            <th>Title</th>
            <th>Status</th>
            <th>Category</th>
            <th>Allow Comments</th>
            <th>Date</th>
            <th>Comments</th>
            <th>View</th>
            <th>Edit</th>
            <th>Delete</th>
          </tr>
        </thead>
        <tbody>
          {{#each posts}}
          <tr>
            <td>{{counter @index}}</td>
            <td>
              {{#if file}}
              <img height="50" width="60" class="img-responsive" src="{{file}}" alt="">
              {{else}}
              <img height="50" width="60" class="img-responsive" src="/src/noImage.png" alt="No Image Available">
              {{/if}}
            </td>
            <td>{{title}}</td>
            <td>{{status}}</td>
            <td>{{category.name}}</td>
            <td>{{allowComments}}</td>
            <td>{{dateFormat date 'MMMM Do YYYY'}}</td>
            <td><a href="/admin/posts/postComment/{{id}}" id="comment" class="fa fa-comments"></a></td>
            <td><a class="fa fa-eye" id="view" href='/post/{{id}}'></a></td>
            <td> <a class="fa fa-pencil-square" id="edit" href='/admin/posts/update/{{id}}'></a></td>
                     <td>
                        <button data-toggle="modal" data-target="#exampleModal" id="delete" onclick="deletePost('{{id}}')"><i class="fa fa-trash"></i></button>
                              
                    </td>
          </tr>
          {{/each}}
        </tbody>
      </table>
    </div>
    {{else}}
    <div>
      <h5 id="headings">{{message}}</h5>
      {{>noData}}
    </div>
    {{/if}}


  </div>
  {{>modal}}
  {{>footer}}
</body>
</html>